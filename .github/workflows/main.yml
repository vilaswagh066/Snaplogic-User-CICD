name: Run Curl Command

on:
  push:
    branches:
      - qa

jobs:
  execute-curl:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Execute Curl Command
        run: |
          curl -X POST "https://elastic.snaplogic.com/api/1/rest/public/assetapi/project/Partners/Nihilent/snaplogic-user-test123" \
               -u "sanghamitra.pattanayak@nihilent.com:Nihilent@1234" \
               -H "Content-Type: application/json" \
               -d '{
                     "permissions": [
                       {
                         "perms": ["R", "W", "A", "X"],
                         "subject_type": "USER",
                         "inherit": true,
                         "subject": "sanghamitra.pattanayak@nihilent.com"
                       }
                     ]
                   }'
